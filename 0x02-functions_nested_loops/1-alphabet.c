#include <stdio.h>
/*void print_alphabet(void);
int main()
{

    puts("ABCDEFGHIJKLMNOPQRSTUVWXYZ");

    return 0;
}

void print_alphabet(void){
    int length = strlen();

    for (int i = 0; i < length; i++)
        s[i] = tolower(s[i]);
}
*/
int main(){
    int letter;
    letter= 'abi';
    putchar(letter);
    letter= 'is';
    putchar(letter);
    letter = 'good';
    putchar(letter);
    return 0;
}